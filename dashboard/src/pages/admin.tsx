export function AdminPage() {
  return (
    <section className="panel">
      <h2 className="section-title">Admin</h2>
      <p className="page-lead">
        Sign-in, users, and API credentials land here in T10.3, after sessions in T10.1. The token control in
        the sidebar stays until that page replaces it.
      </p>
    </section>
  );
}
