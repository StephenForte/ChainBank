export function EmailPage() {
  return (
    <section className="panel">
      <h2 className="section-title">Email</h2>
      <p className="page-lead">
        Triggers, recipients, and the delivery log land here in T10.6, after the deliveries API in T10.5. Send
        test email stays in the top bar until then.
      </p>
    </section>
  );
}
